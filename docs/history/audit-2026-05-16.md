# 2026-05-16 Project Audit — 14/14 Findings Closed (2026-05-18)

**Status (2026-05-18):** Fully remediated. All 14 findings closed in DefectDojo engagement 123 / test 114 (reason=mitigated). Remediation landed across nine commits:
- `0db2431` — Phase 1 (M1, M2, M3, M11)
- `256b6c6` — Phase 2 (H1, H2)
- `dd4e326` — Phase 3 (M4, M7, M8)
- `76d155f` — Phase 4 (M9, M10)
- `b19b5e8` — Phase 5 (M5, M6)
- `28e9711` — Low rollup, part 1 (trim CLAUDE.md → docs/history/)
- `1f589de` — Low rollup, part 2 (CHANGELOG.md + --version)
- `59c6e7b` — Lows-A (README HOOK_AUDIT note, actions/checkout SHA, semgrep comment, token-report-plan archived)
- `764e141` — Lows-B (_RE_PRIVATE_KEY tighten, _unwrap_grouping depth, JSONDecodeError counter, AUTOCLAUDE_MAX_SESSION_MB env, --quiet flag, _canonicalize_pattern dedupe)

Five Low items accepted as wontfix with documented reasoning (symlink TOCTOU → Landlock; CI test sensitive strings → intentional fixtures; hard-coded POSIX paths → Linux focus; _apply_suggestions concurrent-safety → CLI one-shot; _git_commit_count cross-run cache → CLI one-shot). HOME_SLUG-in-JSON was preemptive — render_json already mitigated.

A read-only 12-dimension `/project-audit` run on 2026-05-16 over the application + claude-code domain overlays. 1,006 tests confirmed passing. **0 Critical, 2 High, 11 Medium, 13 Low, 7 Info.** Overall risk posture: Moderate. Findings tracked in DefectDojo engagement 123.

## High (2) — CLOSED in `256b6c6`

H1. **`docker run --mount type=bind,source=...` not detected** — `hooks/block-secrets.py:990-997`. Hook only checked `-v`/`--volume`; the `--mount source=...` syntax bypassed detection. Fix: parsed comma-delimited `--mount` values and ran `source=` through `_is_sensitive_path`. Regression test parallels `hooks/test_block_secrets.py:84`.

H2. **`tar`/`zip` skipped all `-`-prefixed args, missing `--file=` and bunched flags** — `hooks/block-secrets.py:983-988`. `tar -cT/path/...` and `tar --file=...` escaped detection. Fix: extended `_FILE_FLAG_ARGS` with `tar`/`zip` entries for `-T`/`--files-from`/`--file=`, plus bunched-flag handling.

## Medium (11) — CLOSED across `0db2431`, `dd4e326`, `76d155f`, `b19b5e8`

- M1 `_RE_SECRET_ASSIGN` value capture `(\S+)` stopped at first whitespace (quoted values partially redacted) — fixed in `0db2431`
- M2 Env-var truthy parsing was exact-match-to-"1" only (`HOOK_AUDIT=true` silently disabled auditing) — fixed in `0db2431`
- M3 Audit log `summary`/`reason` fields not redacted (only `command` was) — fixed in `0db2431`
- M4 Log-rotation race between parallel hook invocations — fixed in `dd4e326` via `fcntl.flock`
- M5 All session JSONL files held in memory across entire run — `--max-records` cap added in `b19b5e8`
- M6 `render_warns` was O(W × R) substring search — prefix index added in `b19b5e8`
- M7 Audit log had no automatic retention (auto-rotates to `.jsonl.1` but never deletes) — documented single-backup model in `dd4e326`
- M8 No schema validation on `settings.json`/`settings.local.json` loads — `_safe_load_settings()` added in `dd4e326`
- M9 CI base image `node:22-slim` pulled by tag, not digest — pinned in `76d155f`
- M10 No `pyproject.toml` / `setup.py` — added in `76d155f`
- M11 Only first `secret_warning` surfaced; others dropped — `(+N more)` suffix added in `0db2431`

## Low (13) — backlog

Permissive `_RE_PRIVATE_KEY` header; symlink TOCTOU; `HOOK_AUDIT` default-on privacy surface not in README; silent `JSONDecodeError` drops; unbounded `_unwrap_grouping` recursion; `_apply_suggestions` not concurrent-safe; `_git_commit_count` not cached across runs; `HOME_SLUG` username in JSON; CI test stdout includes synthetic sensitive-path strings; `_MAX_SESSION_SIZE` hardcoded; no `CHANGELOG.md`; `CLAUDE.md` is ~700 lines (token waste); misc hygiene (`docs/token-report-plan.md` unlinked, no `--version`/`--quiet`, `actions/checkout@v4` tag-pinned, semgrep no hash verification, append-not-dedupe in apply, hard-coded POSIX paths).

## Info (7) — observations / strengths

Three deliberate copies of token patterns (sync-checked by CI); Read/Write/Edit deny rules vs Bash hook protection symmetry; `.gitleaks.toml` test-file allowlist; no metrics/telemetry; no third-party Python deps; correct `_atomic_write` and POSIX-atomic `O_APPEND` audit log; CLI ergonomics strong; fail-closed default correct (no hook schema-version detection).

## Tracking

- **DefectDojo:** engagement 123 "Project Audit 2026-05-16" under product `autoclaude` (ID 21), test 114. 14 findings: H1=3262, H2=3263, M1-M11=3264-3274, Low-rollup=3275.
- **Vikunja:** P1 #527 (H1), #528 (H2); P3 #529-#539 covering Mediums (M11 deferred to existing #449 for CI coverage); P5 #540, #541 (trim CLAUDE.md, add CHANGELOG + `--version`). Project **#2 Laima**.
- **SilverBullet:** summary in `daily/2026-05-16.md`; remediation in `daily/2026-05-18.md`; project tracking in `1-Projects/Autoclaude/Project Overview.md`.
